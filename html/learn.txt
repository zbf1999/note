//type获取事件类型
//target获取事件目标
//stopPropagation() 阻止事件冒泡
//preventDefault() 阻止事件的默认行为

btn = document.getElementById("btn")
btn.addEventListener("mouseover", demo1)
function demo1() {
	alert(event.type)
}

js内置对象
	string
		indexof()
		match()
		replace()

	Array
		concat() 合并数组
		sort() 排序
		reverse() 数组元素翻转

Dom对象控制HTML
	
js浏览器对象
	window对象
		open(地址，窗口名， 长宽 top,left)
	计时器
		setInterval()-间隔指定的毫秒数不停地执行指定的代码
			var mytime = setInterval(function(){
				getTime()
			}, 1000)
		clearInterval()-停止setInterval
			clearInterval(mytime)
		setTimeout()-暂停指定的毫秒数后执行指定的代码
		clearTimeout()-停止setTimeout
	History对象
		history.back()
		history.forward()
		history.go()
	Location对象
		location.hostname
		~.pathname
		~.port 端口
		~.href
	Screen
		~.availWidth
		~.availHeight
		~.Width
		~.Height
面向对象
	function Person() {
		//初始化对象
	}
	Person.prototype = {
		//对象属性
	}
	继承
		function Student() {

		}
		Student.prototype = new People();
		student继承people
	封装
		(function(){
			函数体
			window.People = People
		}())

定时器管理、函数封装
	parseInt()
		parseInt("30px") -- 30
		

web测试
	